import { Dictionary } from './Dictionary';
export interface SpinnerOptions {
    enabled?: boolean;
    fillColoimport { Dictionary } from './Dictionary';
export interface SpinnerOptions {
    enabled?: boolean;
    fillColor?: string;
    styles?: Dictionary;
}
